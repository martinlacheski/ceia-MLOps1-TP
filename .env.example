PROJECT_NAME=ML-RainOps API

# API / frontend
API_PORT=8000
FRONTEND_PORT=5173
# Generar con: openssl rand -hex 32
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60
DEMO_USERNAME=operador
DEMO_PASSWORD=rainops-dev
DEMO_FULL_NAME=Operador CEML

# PostgreSQL
POSTGRES_PORT=5432
POSTGRES_USER=rainops
POSTGRES_PASSWORD=rainops
POSTGRES_APP_DB=rainops_app
POSTGRES_AIRFLOW_DB=airflow_db
POSTGRES_MLFLOW_DB=mlflow_db

# MinIO / S3
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123
MLFLOW_BUCKET_NAME=mlflow
DATA_REPO_BUCKET_NAME=data

# MLflow
MLFLOW_PORT=5000

# Airflow
AIRFLOW_PORT=8080
AIRFLOW_UID=50000
# Generar con: openssl rand -base64 32
AIRFLOW_API_JWT_SECRET=change-me-airflow-jwt-secret
AIRFLOW_WWW_USER_USERNAME=airflow
AIRFLOW_WWW_USER_PASSWORD=airflow
